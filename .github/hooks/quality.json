{
  "$schema": "https://raw.githubusercontent.com/AI-Huang/schemas/main/copilot-hooks-schema.json",
  "description": "Quality hooks - Run lint and typecheck after edits",
  "hooks": {
    "PostToolUse": [
      {
        "name": "eslint-check",
        "description": "Run focused ESLint after TypeScript/JS changes",
        "command": "bash .github/hooks/lint-edited-file.sh 2>&1 | tail -50",
        "matcher": "editFile",
        "env": {
          "CHECK_FILES": "$TOOL_INPUT_FILES"
        },
        "timeout": 60
      }
    ],
    "Stop": [
      {
        "name": "typecheck-after-edit",
        "description": "Run TypeScript check once at session end",
        "command": "npm run typecheck 2>&1 | tail -30",
        "timeout": 90
      },
      {
        "name": "session-summary",
        "description": "Show modified files at session end",
        "command": "git status --short | head -20"
      }
    ]
  }
}
